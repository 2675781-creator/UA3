<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'Supprimer un article' }) %>
    <body>
        <%- include('../partials/header') %>
            <main class="wrapper">
                <h1>Supprimer un article</h1>
                <form action="/articles/<%= article.id_article %>?_method=DELETE" method="POST" enctype="multipart/form-data">
                    <div class="mb-2">
                        <label for="titre" class="form-label">Titre</label>
                        <input type="text" class="form-control" id="titre" name="titre" value="<%= article.titre %>" readonly>
                    </div>
                    <div class="mb-2">
                        <label for="date_publication" class="form-label">Date de publication</label>
                        <input type="date" class="form-control" id="date_publication" name="date_publication" value="<%= article.date_publication %>" readonly>
                    </div>
                    <div class="mb-2">
                        <label for="status" class="form-label">Status</label>
                        <input type="text" class="form-control" id="status" name="status" value="<%= article.status %>" readonly>
                    </div>
                    <div class="mb-2">
                        <label for="quantite" class="form-label">Quantite</label>
                        <input type="number" class="form-control" id="quantite" name="quantite" value="<%= article.quantite %>" readonly>
                    </div>
                    <div class="mb-2">
                        <label for="id_auteur" class="form-label">identifiant de l'auteur</label>
                        <input required type="number" class="form-control" id="id_auteur" name="id_auteur" value="<%= article.id_auteur %>" readonly>
                    </div>
                    <div class="mb-2">
                        <label for="id_categorie" class="form-label">identifiant de la categorie</label>
                        <input required type="number" class="form-control" id="id_categorie" name="id_categorie" value="<%= article.id_categorie %>" readonly>
                    </div>
                    <div class="mb-2">
                        <label for="id_employe" class="form-label">id de l'employe</label>
                        <input required type="number" class="form-control" id="id_employe" name="id_employe" value="<%= article.id_employe %>" readonly>
                    </div>
                    <button class="btn btn-success" type="submit">Supprimer</button>
                </form>
            </main>
            <%- include('../partials/footer') %>
    </body>
</html>