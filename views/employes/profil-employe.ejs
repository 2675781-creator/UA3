<!DOCTYPE html>
<html lang="fr">
<%- include('../partials/head', { title: 'Profil du employe' }) %>
<body>
    <%- include('../partials/header') %>
    <main class="wrapper">
        <h1>Profil du employe</h1>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= employeData.nom %> <%= employeData.prenom %></h5>
                <p class="card-text">Age <%= employeData.age %></p>
                <p class="card-text">Role <%= employeData.role %></p>
                <p class="card-text">Date embauche <%= employeData.date_embauche %></p>
                <p class="card-text">Numero de tâche<%= employeData.numero_tache %></p>
            </div>
            <div class="card-footer">
                <a href="/employes/edit/<%= employeData.id_employe" class="btn btn-primary">Modifier</a>
                <form action="/employes/<%= employeData.id_employe %>?_method=DELETE" method="POST">
                    <button type="submit" class="btn btn-danger">Supprimer</button>   
                </form>
                <a href="/employes" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>